Assignment-3
************

This code implements a local blockchain system.


To run the program
******************

1. Go the directory where the file is present.

2. To build the program, type make in the terminal.

3. The program has now been built and the executable file a.out is ready.

4. To run the executable file, type ./a.out in the terminal.
   
5. To remove the unnecessary files, type make clean into the terminal
   The executable file a.out and the object files will be removed


Note
****

1. The blocks are not stored on the file system and hence destroyed after execution of the program.

2. For sha256, the standard library code has been used. License file has been attached alongside.


* * * * * * * * Written by Vishakh.S * * * * * * * *
